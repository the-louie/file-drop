version: '3.8'

services:
  file-drop:
    build: .
    container_name: file-drop
    ports:
      - "9898:9898"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./uploads:/app/uploads
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:

